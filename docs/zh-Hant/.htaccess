<IfModule mod_rewrite.c>
  RewriteEngine On

  # Serve real files/directories as-is
  RewriteCond %{REQUEST_FILENAME} -f [OR]
  RewriteCond %{REQUEST_FILENAME} -d
  RewriteRule ^ - [L]

  # SPA fallback -> this folder's own index.html
  RewriteRule ^ index.html [L]
</IfModule>